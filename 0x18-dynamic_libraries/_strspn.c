#include "main.h"
/**
 *_strspn - Gets the length of a prefix substring consisting of only characters
 *from a given set.
 *@s: Pointer to the string to be searched.
 *@accept: Pointer to the string containing the set of characters to match.
 *Return: The number of characters in the initial segment of s which consist
 *only of characters from the string accept.
*/
unsigned int _strspn(char *s, char *accept)
{
return (0);
}
